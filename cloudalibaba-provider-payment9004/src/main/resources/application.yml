server:
  port: 9004


spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
#        server-addr: localhost:8848
        server-addr: localhost:8848 #换成xxx.xxx.xxx.xxx:1111 （nginx）

  #监控端点全部打开
management:
  endpoints:
    web:
      exposure:
        include: "*"
